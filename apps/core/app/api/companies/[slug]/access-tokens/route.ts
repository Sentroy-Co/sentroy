// Company-scoped SDK access tokens (stk_…). Paylaşılan handler; mail/storage
// ile aynı. Core settings sayfasındaki "API Tokens" section bunu çağırır.
export { GET, POST, DELETE } from "@workspace/console/handlers/access-tokens"
